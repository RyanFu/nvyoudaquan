<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <title>女优大全管理后台</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
    <div>
        <% if current_user %>
        <div class="pull-right" style="padding: 10px;;">
          欢迎：<%= current_user.nickname %><small><%= current_user.role %></small>
          -
          <a href="<%= logout_url %>">退出</a>
          -
          <a href="<%= root_url %>">网站首页</a>
        </div>
        <% end %>
        <ul class="nav nav-tabs" style="padding: 10px 20px; padding-bottom: 0;;">
            <li<%= ' class=active' if params[:controller] == 'admin/girls' %>><a href="<%= url_for(:controller => 'admin/girls') %>">女优管理</a></li>
            <li<%= ' class=active' if params[:controller] == 'admin/works' %>><a href="<%= url_for(:controller => 'admin/works') %>">作品维护</a></li>
            <li<%= ' class=active' if params[:controller] == 'admin/users' %>><a href="<%= url_for(:controller => 'admin/users') %>">用户管理</a></li>
        </ul>
    </div>
    <div style="padding: 10px; padding-top: 0;">
      <%= yield %>
    </div>
    <div></div>
</body>
</html>
